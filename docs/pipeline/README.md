# Where `PipelineDocsCli snapshot` writes by default.
#
# Generate:
#   dotnet run --project samples/PipelineDocsCli -- snapshot --solution CSharpCodeAnalyst.sln
#
# `*.pipeline` files are gitignored here (they grow large). Check them into *your*
# product repos when you want a durable reverse-engineering map for rebuild work.
