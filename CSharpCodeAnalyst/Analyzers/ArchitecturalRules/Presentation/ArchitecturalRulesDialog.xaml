<Window x:Class="CSharpCodeAnalyst.Analyzers.ArchitecturalRules.Presentation.ArchitecturalRulesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:resources="clr-namespace:CSharpCodeAnalyst.Resources"
        mc:Ignorable="d"
        Title="Architectural rules configuration"
        Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize" d:DataContext="{d:DesignInstance }">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/ImageStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Define the architectural rules for your solution:"
                   FontSize="14" FontWeight="Bold" Margin="0,0,0,10" />

        <!-- Main text area -->
        <GroupBox Grid.Row="1" Header="Rules Definition" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Toolbar -->
                <DockPanel Grid.Row="0">
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" Margin="0,5,0,5">
                    <Button x:Name="LoadFileButton" Content="Load from File..."
                            Width="120" Height="25" Margin="0,0,10,0"
                            Click="LoadFileButton_Click" />
                    <Button x:Name="SaveFileButton" Content="Save to File..."
                            Width="120" Height="25" Margin="0,0,10,0"
                            Click="SaveFileButton_Click" />
                    <Button x:Name="ClearButton" Content="Clear"
                            Width="80" Height="25"
                            Click="ClearButton_Click" />
                </StackPanel>
                
                <Image Style="{StaticResource InfoStyle}" DockPanel.Dock="Right"
                       ToolTip="{x:Static resources:Strings.ArchitectureRules_Info}" />

                </DockPanel>
                <!-- Rules text box -->
                <TextBox Grid.Row="1" x:Name="RulesTextBox"
                         AcceptsReturn="True"
                         TextWrapping="NoWrap"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         FontFamily="Consolas"
                         FontSize="12"
                         Background="#FAFAFA"
                         Text="{Binding RulesText, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>
        </GroupBox>


        <!-- Buttons -->
     
            
         
            
            <StackPanel  Grid.Row="2"  Orientation="Horizontal"
                         HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="OK" Width="75" Height="25"
                    Margin="0,0,10,0" IsDefault="True"
                    Click="OkButton_Click" />
            <Button x:Name="CancelButton" Content="Cancel" Width="75" Height="25"
                    IsCancel="True"
                    Click="CancelButton_Click" />
        </StackPanel>
        
    </Grid>
</Window>