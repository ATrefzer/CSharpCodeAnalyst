<DataTemplate xmlns='http://schemas.microsoft.com/winfx/2006/xaml/presentation'
              xmlns:x='http://schemas.microsoft.com/winfx/2006/xaml'
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
              xmlns:presentation="clr-namespace:CSharpCodeAnalyst.Analyzers.ArchitecturalRules.Presentation"
              mc:Ignorable="d">
    <ItemsControl 
        d:DataContext="{d:DesignInstance presentation:RuleViolationViewModel }"
        ItemsSource="{Binding RelationshipDetails}">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Grid Margin="40 0 0 0" d:DataContext="{d:DesignInstance presentation:RelationshipViewModel }">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="1"
                               Text="{Binding Description}"
                               Foreground="Blue"
                               TextWrapping="Wrap"
                               Cursor="Hand"
                               TextDecorations="Underline">
                        <TextBlock.InputBindings>
                            <MouseBinding MouseAction="LeftClick"
                                          Command="{Binding DataContext.OpenSourceLocationCommand,
                          RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                          CommandParameter="{Binding }" />
                        </TextBlock.InputBindings>
                    </TextBlock>
                </Grid>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</DataTemplate>