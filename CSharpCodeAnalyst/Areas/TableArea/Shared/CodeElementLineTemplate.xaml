<DataTemplate xmlns='http://schemas.microsoft.com/winfx/2006/xaml/presentation'
              xmlns:x='http://schemas.microsoft.com/winfx/2006/xaml'
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
              xmlns:shared="clr-namespace:CSharpCodeAnalyst.Areas.TableArea.Shared"
              mc:Ignorable="d">
    <ItemsControl ItemsSource="{Binding CodeElements}">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Grid Margin="20 0 0 0" d:DataContext="{d:DesignInstance shared:CodeElementLineViewModel }">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Image Source="{Binding Icon}" Margin="0 1 5 1" />
                    <TextBlock Grid.Column="1" Text="{Binding FullName}"
                               TextWrapping="Wrap" />
                </Grid>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</DataTemplate>